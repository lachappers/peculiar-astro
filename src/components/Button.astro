---
import { Icon } from "astro-icon";

const {
  icon = null,
  text = "Text",
  href,
  buttonColor = "",
  addClasses = "",
} = Astro.props;
---

{
  !icon ? (
    <a class={`button text-lg ${buttonColor} ${addClasses}`} {href}>
      {text}
    </a>
  ) : (
    <a class={`button has-icon text-lg ${buttonColor} ${addClasses}`} {href}>
      {text}
      <Icon name={icon} />
    </a>
  )
}

<style lang="scss"></style>
