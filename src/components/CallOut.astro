---
const { purple, point, subtext } = Astro.props;
---

<div class="animate text-center">
  <p class="text-4xl font-bold">{purple}</p>
  <p class="text-2xl font-semibold">{point}</p>
  <p class="text-xl">{subtext}</p>
</div>

<style lang="scss">
  div > p:first-child {
    color: var(--action-color);
  }
</style>
