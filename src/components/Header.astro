---
import Navigation from "../components/Navigation.astro";
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";

import darkLogo from "@assets/images/350x100-alt-dark.webp";
import lightLogo from "@assets/images/350x100.webp";
import Button from "@components/Button.astro";
import { PAGES } from "@config";
---

<header
  id="page-header"
  class="sticky top-0 z-50 flex h-full w-full max-w-full items-center justify-center px-2 lg:px-4"
>
  <!-- <div class="flex h-full w-full max-w-full py-2">
    <div
      class="container flex max-h-full w-full max-w-full items-center justify-between gap-2"
    > -->
  <!-- <a
        href="/"
        id="nav-logo"
        aria-label="Navigate Home"
        class="flex h-16 min-w-32 flex-auto items-center"
      >
        <Image
          class="block h-full w-fit object-contain dark:hidden"
          src={lightLogo}
          alt=""
          width="350"
          height="100"
        />
        <Image
          class="hidden h-full w-fit object-contain dark:block"
          src={darkLogo}
          alt=""
          width="350"
          height="100"
        />
      </a> -->
  <div
    class="max-w-screen-3xl container relative z-50 flex h-full w-full items-center justify-between gap-4 px-4 py-2 sm:gap-2 sm:px-6 lg:px-8"
  >
    <!-- logo -->

    <a
      href="/"
      aria-label="return to home"
      class="relative flex h-16 max-h-full min-w-fit max-w-fit flex-auto items-center justify-start rounded-md md:max-h-24"
    >
      <Image
        src={lightLogo}
        alt=""
        height="100"
        width="350"
        format="webp"
        class="block h-full w-fit object-contain dark:hidden"
        id="dark-logo"
        loading="eager"
      />
      <Image
        src={darkLogo}
        alt=""
        height="350"
        width="100"
        format="webp"
        class="absolute z-0 hidden h-full w-fit object-contain dark:relative dark:z-auto dark:block"
        id="light-logo"
        loading="eager"
      />
    </a>

    <Navigation />
    <!-- </div> -->
  </div>
</header>

<script></script>

<style lang="scss" is:global>
  @use "@assets/scss/base/breakpoint" as *;
  @use "@assets/scss/base/outline" as *;

  #page-header {
    position: sticky;
    top: 0;

    z-index: 50;

    width: 100%;
    max-height: 7rem;
    background-color: var(--background);
    color: var(--action-color);

    border-bottom: 3px solid var(--font-color);
  }

  .nav-icon {
    @apply h-full w-10 items-center justify-center text-6xl text-[--action-color] transition hover:text-[--secondary-300];
  }

  #main-navigation {
    .has-dropdown {
      position: relative;

      > button {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        padding: 0;
        margin-top: -1px;
        border: none;
        color: var(--action-color);

        &:hover {
          color: var(--action-color-state);

          &::after {
            border-color: var(--action-color-state);
          }
        }

        &::after {
          content: "";
          width: 0.85rem;
          height: 0.75em;
          margin-top: -0.25rem;
          border-style: solid;
          border-width: 0.15em 0.15em 0 0;
          border-color: var(--action-color);
          transform: rotate(135deg);
        }

        &.show {
          &::after {
            margin-top: 0.25rem;
            transform: rotate(-45deg);
          }

          ~ ul {
            display: flex;
            flex-direction: column;
            gap: 1rem;
          }
        }
      }

      ul {
        display: none;
        position: absolute;
        z-index: 100;
        min-width: 260px;
        top: 125%;
        right: 0;
        bottom: auto;
        left: 0;
        padding: 1rem;
        font-color: var(--primary-300);
        background-color: var(--neutral-background);
        border: 3px solid var(--dark-100);
        border-radius: 3px;
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
      }
    }
  }
</style>
